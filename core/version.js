/* core/version.js
   One place for the build tag. Keep this string identical to CACHE in sw.js
   and bump both together on every deploy. Settings → About shows this, so you
   can check at a glance whether the phone is running the latest build:
   it should match the CACHE line in <your-site>/sw.js.
*/
window.APP_BUILD = 'anchor-v11';
